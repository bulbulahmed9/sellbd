import React from 'react'

const Title = ({title}) => {
    return (
        <>
         <h2 style={{ textAlign: 'center', marginBottom: '30px' }}>{title}</h2>   
        </>
    )
}

export default Title
